# Phase 1 — Core dependencies only
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
sqlalchemy==2.0.31
aiosqlite==0.20.0
alembic==1.13.2
python-jose[cryptography]==3.3.0
bcrypt==4.0.1
python-multipart==0.0.9
python-dotenv==1.0.1
httpx==0.27.0
email-validator==2.2.0
# Phase 4 — Investment Optimization Engine
ortools
# Phase 5 — Market Intelligence (uses httpx already in Phase 1)
# Runtime: httpx (already installed), no new backend packages needed
# Dev / Test
pytest
pytest-asyncio
anyio[trio]
